$(function(){
	
	// Init - Hamburger
	$('.hamburger').hamburger({
		negativeSpace: 0, // optional. e.g. ".header". insert element to subtract its height from page height. 
		animation: "sideSlide" // choose "slideDown", "fadeIn", or "sideSlide"
	});

});